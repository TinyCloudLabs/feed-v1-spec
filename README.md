# TinyCloud Feed v1 Spec

Static GitHub Pages site for the Feed v1 implementation spec.

The published document is stored as an encrypted payload. GitHub Pages does not
provide server-side password protection, so access is gated by client-side
decryption in the browser.
